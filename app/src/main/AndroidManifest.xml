<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.suitmedia.suitmediatest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuitmediaTest"
        android:usesCleartextTraffic="true"
        android:name=".MyApplication"
        tools:targetApi="m">
        <activity android:name="com.suitmedia.suitmediatest.ui.event.EventActivity" />
        <activity android:name="com.suitmedia.suitmediatest.ui.guest.GuestActivity" />
        <activity android:name="com.suitmedia.suitmediatest.ui.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>